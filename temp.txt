git branch --show-current
# Expected: dev-45429656

git status --short

# Stage only the CI configuration
git add -- ci-config.yaml

# Check for whitespace errors and review exactly what is staged
git diff --cached --check
git diff --cached --stat
git diff --cached -- ci-config.yaml