# Confirm branch: dev-45429656
git branch --show-current

# Must list ONLY ci-config.yaml
git diff --cached --name-only

git commit -m "ci: configure frontend DevX build and artifact packaging"
if ($LASTEXITCODE -ne 0) { throw "Commit failed" }

git push origin dev-45429656
if ($LASTEXITCODE -ne 0) { throw "Push failed" }