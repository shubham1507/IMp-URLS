setUser({
    ...response.data,
    claims: {
        ...response.data.claims,
        is_platform_admin: !response.data.claims.is_platform_admin,   // ⬅️ reverse here
    },
});


setUser({
    ...user_data,
    claims: {
        ...user_data.claims,
        is_platform_admin: !user_data.claims.is_platform_admin,   // ⬅️ reverse here
    },
});

check

console.log("User data fetched successfully:", response.data);

setUser({
    ...response.data,
    claims: {
        ...response.data.claims,
        is_platform_admin: !response.data.claims.is_platform_admin,  // REVERSED
    },
});


login


let user_data = response.data;

setUser({
    ...user_data,
    claims: {
        ...user_data.claims,
        is_platform_admin: !user_data.claims.is_platform_admin,  // REVERSED
    },
});